{
  "name": "typescript-package-template",
  "version": "1.0.2",
  "description": "Library for using vue hooks with the Agora library",
  "main": "src.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "format": "prettier --write \"src/**/*.ts\"",
    "prepare": "npm run build"
  },
  "author": "Alain Nguyen",
  "license": "ISC",
  "files": [
    "lib/**/*"
  ],
  "devDependencies": {
    "prettier": "^2.4.0",
    "tslint": "^6.1.3",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^4.4.3"
  }
}
